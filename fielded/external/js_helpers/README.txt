To add another js command to the existing ones, write the
command into a js file in this js_helpers directory, and
save it using the format you'd like the entry in the menu
to appear as (replace spaces with underscores).